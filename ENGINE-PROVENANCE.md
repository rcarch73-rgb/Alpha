# Calculation Engine Provenance

Gold Master: `Harbour-North-4.0-RC3-Beta-2.html`

The following four modules were copied unchanged from the Gold Master into `app/js/verified-engine.js`:

- `HNTimeline`
- `HNIncome`
- `HNTax`
- `HNProjection`

The redesigned interface is permitted to translate inputs and display outputs. It must not duplicate or approximate financial calculations.

Scenario rule: clone the complete plan, modify only scenario inputs, and run the same engine.
